#!/usr/bin/env bash

# Command aliases
alias lslrt='ls -lrt'
alias lsa='ls -a'
alias c='clear'

# Quick navigation aliases
alias cddesktop='cd "$HOME/Desktop"'
alias cddownload='cd "$HOME/Downloads"'
alias cddocuments='cd "$HOME/Documents"'
